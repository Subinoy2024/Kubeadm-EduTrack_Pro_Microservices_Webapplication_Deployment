apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: edutrack
  labels:
    app: user-service
    app.kubernetes.io/part-of: edutrack
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
