apiVersion: apps/v1
kind: Deployment
metadata:
  name: recording-service
  namespace: edutrack
  labels:
    app: recording-service
    app.kubernetes.io/part-of: edutrack
spec:
  replicas: 2
  selector:
    matchLabels:
      app: recording-service
  template:
    metadata:
      labels:
        app: recording-service
        app.kubernetes.io/part-of: edutrack
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: recording-service
          image: edutrack/recording-service:latest
          ports:
            - containerPort: 3006
              protocol: TCP
          envFrom:
            - configMapRef:
                name: recording-service-config
            - secretRef:
                name: edutrack-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
