apiVersion: v1
kind: Service
metadata:
  name: course-service
  namespace: edutrack
  labels:
    app: course-service
    app.kubernetes.io/part-of: edutrack
spec:
  type: ClusterIP
  selector:
    app: course-service
  ports:
    - port: 3003
      targetPort: 3003
      protocol: TCP
