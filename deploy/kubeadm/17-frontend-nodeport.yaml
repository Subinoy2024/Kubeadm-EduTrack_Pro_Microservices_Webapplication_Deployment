apiVersion: v1
kind: Service
metadata:
  name: frontend-nodeport
  namespace: edutrack
spec:
  selector:
    app: frontend
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30091
      protocol: TCP
