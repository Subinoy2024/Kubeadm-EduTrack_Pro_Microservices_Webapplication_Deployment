apiVersion: v1
kind: Service
metadata:
  name: ai-chat-service
  namespace: edutracklocal
spec:
  selector:
    app: ai-chat-service
  ports:
    - name: http
      port: 3007
      targetPort: 3007
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-chat-service
  namespace: edutracklocal
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: ai-chat-service
  template:
    metadata:
      labels:
        app: ai-chat-service
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: ai-chat-service
          image: YOUR_IMAGE_REGISTRY/edutrack-ai-chat-service:YOUR_IMAGE_TAG
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3007
              name: http
          env:
            - name: PORT
              value: "3007"
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: edutrack-config
                  key: NODE_ENV
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: edutrack-secrets
                  key: DATABASE_URL
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: edutrack-secrets
                  key: JWT_SECRET
            - name: AI_PROVIDER
              valueFrom:
                configMapKeyRef:
                  name: edutrack-config
                  key: AI_PROVIDER
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]
          readinessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
