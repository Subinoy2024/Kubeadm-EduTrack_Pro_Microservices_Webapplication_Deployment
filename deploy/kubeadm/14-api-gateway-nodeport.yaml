apiVersion: v1
kind: Service
metadata:
  name: api-gateway-nodeport
  namespace: edutrack
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30090
